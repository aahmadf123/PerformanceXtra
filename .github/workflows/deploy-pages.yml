name: Deploy to GitHub Pages

# Publishes the static site (index.html + assets at the repo root) to GitHub Pages.
# One-time setup: in the repo, go to Settings -> Pages -> Build and deployment ->
# Source: "GitHub Actions". After that, every push to main redeploys automatically.
# You can also run it manually on any branch via the "Run workflow" button.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment; cancel in-progress runs of this group.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # The whole repo root is the static site. build/ is dev-only but harmless
          # to upload; index.html is what gets served.
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
